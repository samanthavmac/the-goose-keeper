# Copy to .env for local runs only. Never commit .env.
# On Render: set these in the service Environment tab (not in git).

DATABASE_URL=postgresql://user:password@localhost:5432/goose_keeper
GOOSE_EGG_LIMIT=10
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/REPLACE/WITH/YOUR_WEBHOOK
